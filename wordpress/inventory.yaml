wordpress:
  hosts:
    - 172.25.0.3

pruebas:
  hosts:
    - 0.0.0.0

infra:
  children:
    - wordpress
    - prueba

Wordpress:
  vars:
    ansible_user: root
    ansible_ssh_private_key_file: /root/.ssh/id_rsa
    # opc 3: añadir variables en el inventario
    #mariadb_root_password: "mypassword"
    #wp_db_name: "wordpress"
    #wp_user: "wordpress"
    #wp_password: "password"